# Offline Voice Translator - Minimal Dependencies for Railway
# Heavy packages (Vosk, Argos) install at runtime to avoid build timeout

# Web Framework
Flask==3.1.2
Flask-SocketIO==5.5.1
python-engineio==4.11.2
python-socketio==5.12.1
eventlet==0.36.1

# Core dependencies only - models install at startup
vosk==0.3.45
argostranslate==1.10.0

# Utilities
requests==2.32.5
gunicorn==21.2.0
