# 100% Open Source Voice Translation Dependencies
SpeechRecognition==3.10.0
argostranslate==1.9.1
pyttsx3==2.90
pyaudio==0.2.14

# Optional: Vosk for better offline speech recognition
# Download models from: https://alphacephei.com/vosk/models
vosk==0.3.45

# Optional: PocketSphinx for offline speech recognition (fallback)
pocketsphinx==5.0.3
