{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pip3 install -r requirements.txt"
  },
  "deploy": {
    "startCommand": "cd web && python app_offline.py",
    "restartPolicyType": "ON_FAILURE"
  }
}
